word        [[:graph:]]+
%%
house/(boat|hold)           {
                              std::cout << matched() << '\n';
                            }
{word}                        {
                              std::cout << matched() << '\n';
                            }
.